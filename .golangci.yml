linters:
  enable:
  # golangci-lint defaults 
  # ref: https://golangci-lint.run/usage/linters/#enabled-by-default
  - errcheck
  - gosimple
  - govet
  - ineffassign
  - staticcheck
  - unused
  # additional
  - misspell
  - gofmt
  fast: true
run:
  timeout: 5m